server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:iwa
    username: sa
    password: password

app:
  invalidPasswordList: "/invalid-password-list.txt"
